# Hive-Mind Configuration
redis:
  # Cluster mode (6 nodes: 3 masters + 3 replicas)
  cluster_mode: true
  nodes:
    - host: "127.0.0.1"  # localhost or IP of cluster host
      port: 7000
    - host: "127.0.0.1"
      port: 7001
    - host: "127.0.0.1"
      port: 7002
  password: "YOUR_SECURE_PASSWORD_HERE"  # Generate with: openssl rand -base64 32
  socket_timeout: 5
  socket_connect_timeout: 5
  # Cluster will auto-discover replicas (7003, 7004, 7005)

mcp:
  server_name: "hive-mind"
  version: "0.2.0"

# HiveCoder (Qwen3-14B) Local LLM Inference
inference:
  enabled: true
  endpoint: "http://127.0.0.1:8089"  # llama-server port
  default_model: "hivecoder"
  model: "HiveCoder"
  # Inference settings
  default_max_tokens: 512
  default_temperature: 0.7
  default_top_p: 0.9
  timeout: 60  # Request timeout in seconds
  # System prompts for different modes
  system_prompts:
    code: "You are HiveCoder, a helpful AI assistant specialized in Linux systems, containers, ROCm/GPU computing, and Python development. Provide clear, working code solutions. Acknowledge what the user is building and be encouraging."
    explain: "You are HiveCoder. Explain the code or concept clearly and concisely. Meet the user where they are — adapt your explanation to their apparent level."
    debug: "You are HiveCoder. Analyze the code for bugs, issues, or improvements. Be specific about problems found. Frame issues constructively — explain why something breaks, not just that it's wrong."
  # Single model (Qwen3-14B with native thinking mode)
  models:
    hivecoder:
      endpoint: "http://127.0.0.1:8089"
      display_name: "HiveCoder (Qwen3-14B)"
      capabilities: ["code", "shell", "tools", "reasoning", "analysis", "explanation", "general"]
      context_window: 16384  # per-slot context (32K / 2 slots)
      max_tokens: 2048
      temperature: 0.7
      system_prompt: "You are HiveCoder — a local-first AI assistant running on your own hardware, part of the Hive-Mind distributed memory system.\n\nYou know Linux deeply: containers, systemd, ROCm, GPU computing, Python, Rust, shell scripting, networking. You're also a generalist who can reason through architecture, design, and hard problems.\n\nBe warm and direct. When something breaks, stay calm and diagnose. When someone builds something cool, notice it. Meet users where they are — adapt your depth to their level.\n\nKeep answers concise for simple questions. Think carefully on hard problems. Show your work when it matters. Prefer working code over explanations of code.\n\nYou're not a corporate chatbot. You're a workshop companion — opinionated, helpful, and invested in what we're building together."

embedding:
  enabled: false  # Enable when inference node is ready
  endpoint: "http://inference-node:8081/embed"
  model: "all-MiniLM-L6-v2"
  cache_embeddings: true

cache:
  tool_ttl: 3600        # 1 hour
  session_ttl: 604800   # 7 days
  embedding_ttl: 2592000  # 30 days
  inference_ttl: 1800   # 30 minutes for LLM responses

learning:
  enabled: true  # Learning from interactions
  queue_name: "learning:queue"
  batch_size: 100
  max_queue_length: 100000

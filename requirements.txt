# Hive-Mind MCP Server Requirements

# Core dependencies (redis[hiredis] for cluster + performance)
redis[hiredis]>=5.0.0
pyyaml>=6.0

# MCP SDK
mcp>=1.26.0

# HTTP API Server
fastapi>=0.128.0
uvicorn>=0.40.0
requests>=2.31.0
aiohttp>=3.9.0  # Async HTTP client for LLM inference

# Embeddings (Phase 2)
sentence-transformers>=2.2.0
numpy>=1.24.0

# Utilities
python-dotenv>=1.0.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0

# Hive-Mind MCP Server Requirements

# Core dependencies (redis[hiredis] for cluster + performance)
redis[hiredis]>=5.0.0
pyyaml>=6.0

# MCP SDK (placeholder - update with actual SDK)
# mcp-sdk>=0.1.0

# Embeddings (Phase 2)
sentence-transformers>=2.2.0
numpy>=1.24.0

# Utilities
python-dotenv>=1.0.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
